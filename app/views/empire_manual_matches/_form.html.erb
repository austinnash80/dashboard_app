
<%= simple_form_for(@empire_manual_match) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :match %>
    <%= f.input :uid %>
    <%= f.input :lid %>
    <%= f.input :list %>
    <%= f.input :lic %>
    <%= f.input :lname %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
